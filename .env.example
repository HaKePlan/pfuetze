# Copy to .env and fill in secrets. That file is gitignored.
# just reads .env automatically before every target.

# ── OpenTofu / bpg/proxmox ───────────────────────────────────────────────────
PROXMOX_VE_PASSWORD=your-proxmox-password

# ── Packer (proxmox-iso) ─────────────────────────────────────────────────────
# Proxmox password for gigu@pam
PKR_VAR_proxmox_password=your-proxmox-password
